# Copy to .env and set values for production/local
# MongoDB connection string (default uses local MongoDB)
MONGODB_URI=mongodb://127.0.0.1:27017/library-app

# JWT secret used to sign tokens - change in production
JWT_SECRET=change_this_secret

# Server port (optional)
PORT=5000
